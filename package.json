{
  "name": "homebridge-sh-switch",
  "version": "1.0.0",
  "description": "Define homebridge switch accessories using shell commands.",
  "keywords": [
    "homebridge-plugin",
    "sh",
    "shell",
    "switch"
  ],
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && tsc",
    "lint": "prettier -c ./src/**/*",
    "format": "prettier -w ./src/**/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marco Zernetsch <m.zernetsch@gmail.com>",
  "license": "ISC",
  "engines": {
    "node": ">=12.13.0",
    "homebridge": ">=1.3.0"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "homebridge": "^1.3.4",
    "prettier": "^2.4.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
